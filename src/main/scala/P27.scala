package org.tritsch.scala.ninetynine

/**
 * P27 - group the elements of a set into disjoint subsets
 */

object P27 {
  final def group3(l: List[Any]): List[List[List[Any]]] = {
    List(List(List()))
  }

  final def group(gs: List[Int], l: List[Any]): List[List[List[Any]]] = {
    List(List(List()))
  }
}
