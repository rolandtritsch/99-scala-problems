package org.tritsch.scala.ninetynine

/**
 * P28 - group the elements of a set into disjoint subsets
 */

object P28 {
  final def lsort(l: List[List[Any]]): List[List[Any]] = {
    List(List())
  }

  final def lsortFreq(l: List[List[Any]]): List[List[Any]] = {
    List(List())
  }
}
